# For initial setup rename me to "config.yml"
#
#
include:
  - autogenerated.config.yml

app:
  basepath:
  index: home
  name: Demo
  banner: images/logo.jpg

  controllers:
    - controller
  model: model
  views:
    - view
    - view/includes
  filesystem: data
  routes:
    #- MyNameSpace\home: ["/home/example/[i:id]", GET]
    #- home: ["/home/example/[i:id]", GET]
    #- sub_home: ["/home/sub/example/[i:id]", GET]

database:
  cache:
    connector: GLFramework\Cache\MemoryCache
    host: localhost


modules:
  internal:
    - csrf
    - installer
    - admin
    - login
